---
import { languages, getLangFromUrl, getRouteFromUrl, getLocalizedPath } from '../i18n/utils';

const currentLang = getLangFromUrl(Astro.url);
const currentRoute = getRouteFromUrl(Astro.url);
---

<div class="lang-switch">
  {Object.entries(languages).map(([lang, label]) => (
    <a
      href={getLocalizedPath(currentRoute, lang as keyof typeof languages)}
      class:list={['lang-option', { active: currentLang === lang }]}
      aria-label={`Canviar a ${label}`}
    >
      {lang.toUpperCase()}
    </a>
  ))}
</div>

<style>
  .lang-switch {
    display: flex;
    gap: 2px;
    font-family: var(--f-mono);
    font-size: 10px;
    letter-spacing: .1em;
    background: rgba(255,255,255,0.1);
    padding: 4px;
    border-radius: 6px;
  }

  .lang-option {
    padding: 6px 8px;
    border-radius: 4px;
    color: rgba(255,255,255,0.5);
    transition: color 0.2s, background 0.2s;
  }

  .lang-option:hover {
    color: var(--bone);
  }

  .lang-option.active {
    color: var(--bone);
    background: rgba(255,255,255,0.15);
  }

  /* Quan el nav te fons clar (on-bone) */
  :global(.nav.on-bone) .lang-switch {
    background: rgba(0,0,0,0.08);
  }

  :global(.nav.on-bone) .lang-option {
    color: rgba(45,42,38,0.5);
  }

  :global(.nav.on-bone) .lang-option:hover {
    color: var(--bone-ink);
  }

  :global(.nav.on-bone) .lang-option.active {
    color: var(--bone-ink);
    background: rgba(0,0,0,0.12);
  }

  /* Mobile nav */
  :global(.nav-mobile) .lang-switch {
    background: rgba(255,255,255,0.1);
  }

  :global(.nav-mobile) .lang-option {
    color: rgba(255,255,255,0.6);
  }

  :global(.nav-mobile) .lang-option:hover {
    color: var(--bone);
  }

  :global(.nav-mobile) .lang-option.active {
    color: var(--bone);
    background: rgba(255,255,255,0.2);
  }
</style>
